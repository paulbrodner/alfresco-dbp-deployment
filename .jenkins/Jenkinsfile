pipeline {
  agent {
    label "dbp"
  }

  options {        
    ansiColor('xterm')
  }

  environment { 
    DESIREDNAMESPACE = "`${env.GIT_BRANCH}| sed 's/\//_/'`-${env.BUILD_NUMBER}"
    DNSZONE          = "${DESIREDNAMESPACE}.dev.alfresco.me"
  }

  stages {
    stage('Namespace') {
      steps {
        sh "env"        
      }
    }
  }
}
