pipeline {
  agent {
    label "dbp"
  }

  options {        
    ansiColor('xterm')
  }

  environment { 
    DESIREDNAMESPACE = "${env.BRANCH_NAME}-${env.BUILD_NUMBER}"
    DNSZONE          = "${DESIREDNAMESPACE}.dev.alfresco.me"
  }

  stages {
    stage('Namespace') {
      steps {
        sh "env"        
      }
    }
  }
}
